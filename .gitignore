# Hardware-specific (don't share)
hardware-configuration.nix

# Build artifacts
result
result-*

# Backup files
*.backup
*.bak
*~

# Nix build outputs
.direnv/

# Claude Code: shared project config is committed, personal overrides are not
.claude/settings.local.json
